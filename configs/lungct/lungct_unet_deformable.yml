experiment_name: 'lungct_unet_deformable'

# data
data:
  name: 'lungct'
  path: '../../Datasets/LungCT/'
  crop_x: null
  crop_y: null
  crop_z: null

# network configs
model:
  model_type: 'unet'
  loss_type: 'ngf'
  architecture:
    down_channels: [32, 32, 64, 256]
    up_channels: [256, 64, 32, 32]
    decoder_only: true
    use_attention_gates: true
    use_se_attention: true
    time_emb_dim: 128

# training configs
training:
  batch_size: 1
  res_levels: 8
  lr: 1e-4
  epochs: 400
  eval_interval: 1
  checkpoints_path: 'checkpoints/lungct2.pt'
  logdir: 'logs/lungct/lungct2.log'

# loss factors
loss:
  image_loss_coeff: 1.
  semigroup_coeff: 1e4

# evaluation configs
test:
  down_factor: 2
  save_evals: true
  save_evals_path: 'evals/lungct/lungct2'
  logdir: 'logs/lungct/lungct2_eval.log'
